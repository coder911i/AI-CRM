import { Job } from 'bull';
export declare class EmailWorker {
    private transporter;
    send(job: Job<{
        to: string;
        subject: string;
        html: string;
        from?: string;
    }>): Promise<void>;
}
