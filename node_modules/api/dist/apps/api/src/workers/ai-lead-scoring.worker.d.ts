import { Job } from 'bull';
import { PrismaService } from '../common/prisma/prisma.service';
import { EventsGateway } from '../gateways/events.gateway';
import { AIService } from '../common/ai/ai.service';
export declare class AiLeadScoringWorker {
    private prisma;
    private events;
    private ai;
    constructor(prisma: PrismaService, events: EventsGateway, ai: AIService);
    scoreLead(job: Job<{
        leadId: string;
        tenantId: string;
    }>): Promise<void>;
}
