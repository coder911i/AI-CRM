import { Queue } from 'bull';
import { PrismaService } from '../common/prisma/prisma.service';
export declare class VisitReminderWorker {
    private prisma;
    private emailQueue;
    constructor(prisma: PrismaService, emailQueue: Queue);
    send24hReminders(): Promise<void>;
    send2hReminders(): Promise<void>;
}
