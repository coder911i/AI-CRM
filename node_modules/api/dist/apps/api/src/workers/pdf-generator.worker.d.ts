import { Job } from 'bull';
import { PrismaService } from '../common/prisma/prisma.service';
export declare class PdfGeneratorWorker {
    private prisma;
    private r2;
    constructor(prisma: PrismaService);
    generateBookingConfirmation(job: Job<{
        bookingId: string;
    }>): Promise<{
        url: string;
    }>;
}
