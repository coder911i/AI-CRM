import { Queue } from 'bull';
import { PrismaService } from '../common/prisma/prisma.service';
import { AIService } from '../common/ai/ai.service';
export declare class AiFollowUpWorker {
    private prisma;
    private ai;
    private emailQueue;
    private readonly logger;
    constructor(prisma: PrismaService, ai: AIService, emailQueue: Queue);
    processSequences(): Promise<void>;
    sendPriceDropAlert(projectId: string, newPrice: number): Promise<void>;
    sendInventoryUrgency(projectId: string, remainingUnits: number): Promise<void>;
    private sendFollowUpEmail;
}
