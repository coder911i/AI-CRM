import { PrismaService } from '../prisma/prisma.service';
export declare class AuditService {
    private prisma;
    constructor(prisma: PrismaService);
    log(tenantId: string, action: string, entity: string, entityId: string, userId?: string, oldData?: any, newData?: any, ipAddress?: string): Promise<any>;
}
