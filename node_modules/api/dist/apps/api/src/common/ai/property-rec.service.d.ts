import { PrismaService } from '../prisma/prisma.service';
import { AIService } from './ai.service';
export declare class PropertyRecService {
    private prisma;
    private ai;
    private readonly logger;
    constructor(prisma: PrismaService, ai: AIService);
    recommendUnits(tenantId: string, leadId: string, topK?: number): Promise<any>;
    syncUnitEmbedding(unitId: string): Promise<void>;
}
