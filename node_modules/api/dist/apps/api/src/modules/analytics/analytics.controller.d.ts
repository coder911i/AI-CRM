import { AnalyticsService } from './analytics.service';
import { JwtPayload } from '@waterting/shared';
export declare class AnalyticsController {
    private analytics;
    constructor(analytics: AnalyticsService);
    getInventory(user: JwtPayload, projectId?: string): Promise<{
        statusBreakdown: {
            TOTAL: any;
            AVAILABLE: any;
            BOOKED: any;
            SOLD: any;
            RESERVED: any;
        };
        totalSalesValue: any;
        absorptionRate30d: any;
        occupancyPct: number;
    }>;
}
