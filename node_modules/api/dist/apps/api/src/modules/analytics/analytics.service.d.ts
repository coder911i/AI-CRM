import { PrismaService } from '../../common/prisma/prisma.service';
export declare class AnalyticsService {
    private prisma;
    constructor(prisma: PrismaService);
    getInventoryInsights(tenantId: string, projectId?: string): Promise<{
        statusBreakdown: {
            TOTAL: any;
            AVAILABLE: any;
            BOOKED: any;
            SOLD: any;
            RESERVED: any;
        };
        totalSalesValue: any;
        absorptionRate30d: any;
        occupancyPct: number;
    }>;
}
