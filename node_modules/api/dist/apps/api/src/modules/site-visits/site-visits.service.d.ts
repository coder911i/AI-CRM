import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload, VisitOutcome } from '@waterting/shared';
export declare class SiteVisitsService {
    private prisma;
    constructor(prisma: PrismaService);
    schedule(user: JwtPayload, data: any): Promise<any>;
    recordOutcome(user: JwtPayload, id: string, outcome: VisitOutcome, notes?: string): Promise<any>;
    findAll(user: JwtPayload): Promise<any>;
}
