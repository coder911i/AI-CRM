export declare class MediaService {
    private r2;
    constructor();
    upload(tenantId: string, module: string, entityId: string, file: Express.Multer.File): Promise<{
        url: string;
        key: string;
    }>;
}
