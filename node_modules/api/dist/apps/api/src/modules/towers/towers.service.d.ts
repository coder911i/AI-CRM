import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload } from '@waterting/shared';
export declare class TowersService {
    private prisma;
    constructor(prisma: PrismaService);
    create(user: JwtPayload, projectId: string, data: any): Promise<any>;
    findAllByProject(user: JwtPayload, projectId: string): Promise<any>;
    update(user: JwtPayload, id: string, data: any): Promise<any>;
}
