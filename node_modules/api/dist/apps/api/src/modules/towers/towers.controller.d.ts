import { TowersService } from './towers.service';
import { JwtPayload } from '@waterting/shared';
export declare class TowersController {
    private readonly towersService;
    constructor(towersService: TowersService);
    create(user: JwtPayload, projectId: string, createTowerDto: any): Promise<any>;
    findAll(user: JwtPayload, projectId: string): Promise<any>;
    update(user: JwtPayload, id: string, data: any): Promise<any>;
}
