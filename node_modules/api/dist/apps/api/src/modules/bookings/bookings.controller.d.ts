import { BookingsService } from './bookings.service';
import { JwtPayload } from '@waterting/shared';
import { PrismaService } from '../../common/prisma/prisma.service';
export declare class BookingsController {
    private readonly bookingsService;
    private prisma;
    constructor(bookingsService: BookingsService, prisma: PrismaService);
    create(user: JwtPayload, createBookingDto: any): Promise<any>;
    findAll(user: JwtPayload, page?: number, limit?: number): Promise<any>;
    findOne(user: JwtPayload, id: string): Promise<any>;
    schedule(user: JwtPayload, id: string, payments: any[]): Promise<any>;
    recordPayment(user: JwtPayload, id: string, paymentId: string, data: any): Promise<any>;
    exportPayments(user: JwtPayload, id: string, res: any): Promise<void>;
}
