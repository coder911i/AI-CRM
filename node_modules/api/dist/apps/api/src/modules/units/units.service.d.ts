import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload, UnitStatus } from '@waterting/shared';
import { AiFollowUpWorker } from '../../workers/ai-follow-up.worker';
export declare class UnitsService {
    private prisma;
    private aiFollowUp;
    constructor(prisma: PrismaService, aiFollowUp: AiFollowUpWorker);
    create(user: JwtPayload, towerId: string, data: any): Promise<any>;
    findAllByTower(user: JwtPayload, towerId: string): Promise<any>;
    updateStatus(user: JwtPayload, id: string, status: UnitStatus, holdUntil?: Date): Promise<any>;
    updatePrice(user: JwtPayload, id: string, newPrice: number): Promise<any>;
}
