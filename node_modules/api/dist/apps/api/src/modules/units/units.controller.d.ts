import { UnitsService } from './units.service';
import { JwtPayload, UnitStatus } from '@waterting/shared';
import { PrismaService } from '../../common/prisma/prisma.service';
export declare class UnitsController {
    private readonly unitsService;
    private prisma;
    constructor(unitsService: UnitsService, prisma: PrismaService);
    create(user: JwtPayload, towerId: string, createUnitDto: any): Promise<any>;
    findAll(user: JwtPayload, towerId: string): Promise<any>;
    bulkPrice(user: JwtPayload, dto: {
        towerId: string;
        floor?: number;
        basePrice: number;
    }): Promise<any>;
    holdUnit(user: JwtPayload, id: string, dto: {
        holdHours: 48 | 72;
    }): Promise<any>;
    updateStatus(user: JwtPayload, id: string, status: UnitStatus, holdUntil?: string): Promise<any>;
    exportUnits(user: JwtPayload, projectId: string, res: any): Promise<void>;
}
