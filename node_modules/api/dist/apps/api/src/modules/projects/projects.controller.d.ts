import { ProjectsService } from './projects.service';
import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload } from '@waterting/shared';
export declare class ProjectsController {
    private readonly projectsService;
    private prisma;
    constructor(projectsService: ProjectsService, prisma: PrismaService);
    create(user: JwtPayload, createProjectDto: any): Promise<any>;
    findAll(user: JwtPayload, page?: number, limit?: number): Promise<any>;
    exportUnits(user: JwtPayload, id: string, res: any): Promise<void>;
    findOne(user: JwtPayload, id: string): Promise<any>;
    update(user: JwtPayload, id: string, updateProjectDto: any): Promise<any>;
    remove(user: JwtPayload, id: string): Promise<any>;
}
