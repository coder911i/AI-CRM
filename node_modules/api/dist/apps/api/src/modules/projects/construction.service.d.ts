import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload } from '@waterting/shared';
export declare class ConstructionService {
    private prisma;
    constructor(prisma: PrismaService);
    createUpdate(user: JwtPayload, projectId: string, data: any): Promise<any>;
    getProjectFeed(projectId: string): Promise<any>;
    getBuyerFeed(buyerEmail: string): Promise<any>;
}
