import { AutomationsService } from './automations.service';
import { JwtPayload } from '@waterting/shared';
export declare class AutomationsController {
    private readonly automationsService;
    constructor(automationsService: AutomationsService);
    create(user: JwtPayload, data: any): Promise<any>;
    findAll(user: JwtPayload): Promise<any>;
    toggle(user: JwtPayload, id: string): Promise<any>;
}
