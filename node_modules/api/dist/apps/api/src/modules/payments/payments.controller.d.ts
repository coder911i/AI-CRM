import { Response } from 'express';
import { PaymentsService } from './payments.service';
import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload } from '@waterting/shared';
export declare class PaymentsController {
    private readonly paymentsService;
    private prisma;
    constructor(paymentsService: PaymentsService, prisma: PrismaService);
    create(user: JwtPayload, bookingId: string, data: any): Promise<any>;
    findAll(user: JwtPayload, bookingId: string): Promise<any>;
    recordPayment(user: JwtPayload, id: string, data: any): Promise<any>;
    exportPayments(user: JwtPayload, bookingId: string, res: Response): Promise<void>;
}
