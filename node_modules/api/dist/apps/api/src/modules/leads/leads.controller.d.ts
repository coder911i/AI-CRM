import { LeadsService } from './leads.service';
import { JwtPayload } from '@waterting/shared';
import { PrismaService } from '../../common/prisma/prisma.service';
import { AIService } from '../../common/ai/ai.service';
export declare class LeadsController {
    private readonly leadsService;
    private prisma;
    private ai;
    constructor(leadsService: LeadsService, prisma: PrismaService, ai: AIService);
    create(user: JwtPayload, createLeadDto: any): Promise<any>;
    findAll(user: JwtPayload, page?: number, limit?: number): Promise<any>;
    findOne(user: JwtPayload, id: string): Promise<any>;
    update(user: JwtPayload, id: string, data: any): Promise<any>;
    assign(user: JwtPayload, id: string, userId: string): Promise<any>;
    addNote(user: JwtPayload, id: string, title: string, description: string): Promise<any>;
    addCall(user: JwtPayload, id: string, duration: string, outcome: string): Promise<any>;
    updateStage(user: JwtPayload, id: string, stage: string): Promise<any>;
    unsubscribe(id: string, req: any): Promise<{
        message: string;
    }>;
    publicUnsubscribe(leadId: string, res: any): Promise<void>;
    bulkAction(dto: any, req: any): Promise<{
        updated: any;
    }>;
    exportCSV(query: any, req: any, res: any): Promise<void>;
    getAIBrief(id: string, user: JwtPayload): Promise<unknown>;
    getRecommendations(id: string, user: JwtPayload): Promise<any>;
    getAISummary(id: string, user: JwtPayload): Promise<{
        summary: string;
    }>;
}
