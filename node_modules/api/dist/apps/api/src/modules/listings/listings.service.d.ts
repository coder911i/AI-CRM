import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload } from '@waterting/shared';
export declare class ListingsService {
    private prisma;
    constructor(prisma: PrismaService);
    create(user: JwtPayload, data: any): Promise<any>;
    findAll(user: JwtPayload): Promise<any>;
    syncToPlatform(listingId: string): Promise<any>;
    handleWebhook(tenantId: string, platform: string, payload: any): Promise<any>;
    bulkUpdatePrices(tenantId: string, projectId: string, increasePct: number): Promise<any>;
}
