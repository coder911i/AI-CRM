import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload } from '@waterting/shared';
import { AuditService } from '../../common/audit/audit.service';
export declare class BrokerDisputeService {
    private prisma;
    private audit;
    constructor(prisma: PrismaService, audit: AuditService);
    create(user: JwtPayload, data: any): Promise<any>;
    resolve(user: JwtPayload, id: string, data: {
        resolution: string;
        status: string;
    }): Promise<any>;
    findAll(user: JwtPayload): Promise<any>;
}
