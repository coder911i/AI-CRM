import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload } from '@waterting/shared';
export declare class BrokerDisputesController {
    private prisma;
    constructor(prisma: PrismaService);
    findAll(brokerId: string, user: JwtPayload): Promise<any>;
    create(brokerId: string, user: JwtPayload, dto: {
        leadId: string;
        reason: string;
    }): Promise<any>;
    resolve(id: string, dto: {
        resolution: string;
        status: 'RESOLVED' | 'REJECTED';
    }): Promise<any>;
}
