import { Response } from 'express';
import { BrokersService } from './brokers.service';
import { PrismaService } from '../../common/prisma/prisma.service';
import { JwtPayload } from '@waterting/shared';
export declare class BrokersController {
    private readonly brokersService;
    private prisma;
    constructor(brokersService: BrokersService, prisma: PrismaService);
    create(user: JwtPayload, createBrokerDto: any): Promise<any>;
    findAll(user: JwtPayload, page?: number, limit?: number): Promise<any>;
    findOne(user: JwtPayload, id: string): Promise<any>;
    approve(id: string, user: JwtPayload): Promise<any>;
    getQR(id: string, res: Response): Promise<Response<any, Record<string, any>>>;
    getStatement(user: JwtPayload, id: string): Promise<{
        broker: {
            name: any;
            email: any;
            phone: any;
        };
        summary: {
            totalCommission: any;
            paidCommission: any;
            pendingCommission: number;
        };
        transactions: any;
    }>;
}
