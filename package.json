{
  "name": "ai-crm-root",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "waterting",
    "waterting/apps/*",
    "waterting/packages/*"
  ],
  "scripts": {
    "migrate:prod": "npx prisma migrate deploy --schema waterting/prisma/schema.prisma || npx prisma db push --schema waterting/prisma/schema.prisma --accept-data-loss",
    "build:api": "npm install && cd waterting/apps/api && npx nest build && cd ../../.. && (rm -rf dist || true) && cp -r waterting/apps/api/dist dist",
    "start:api": "npm run migrate:prod && node dist/main"
  }
}
