{
  "name": "ai-crm-root",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "waterting",
    "waterting/apps/*",
    "waterting/packages/*"
  ],
  "scripts": {
    "build:api": "npm install && npm run build -w api",
    "migrate:prod": "npx prisma migrate deploy --schema waterting/prisma/schema.prisma || npx prisma db push --schema waterting/prisma/schema.prisma --accept-data-loss",
    "start:api": "npm run migrate:prod && node waterting/apps/api/dist/apps/api/src/main"
  }
}
